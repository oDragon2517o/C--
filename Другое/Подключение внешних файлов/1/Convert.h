#pragma once

#include <string>
#include <vector>
#include <fstream>
using namespace std;

class Convert
{
private:
public:
    Convert(string p_fileName = "hello.txt", string p_x6U = "x6U.txt", string p_x210 = "x210.txt"); // Передает имя файла
};